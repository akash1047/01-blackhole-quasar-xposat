[project]
name = "xposat"
version = "0.1.0"
description = "Black hole, quasar, and XPoSat analysis"
requires-python = ">=3.14"
dependencies = [
    "astropy>=8.0.1",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
]
